  /*  async function loadPosts() {
    try {
        const response = await fetch('http://localhost:3001/api/'); 
        const posts = await response.json(); 

        posts.forEach(post => {
            const postsContainer = document.querySelector('.events__list');

            postsContainer.insertAdjacentHTML('afterbegin', `<div class="events__item_w"> <article class="events__item">
                    <h2 class="events__item_title">${post.title}</h2>
                    <div class="events__item_descr">${post.description}</div>
                    <div class="events__item_event_date">${post.date}</div>
                    <div class="events__item_organizer">${post.organizer}</div>
                    <div class="events__btn">
                        <a href="registr.html?postId=${post._id}" class="events__registr_btn">Registr</a>
                        <a href="registr_list.html?postId=${post._id}" class="events__view_btn">View</a>
                    </div>
                </article> </div>`)
        });
    } catch (error) {
        console.error('Ошибка загрузки постов:', error);
    }
}

window.onload = loadPosts;  */
/* 
document.querySelector('.registr__form').addEventListener('submit', async (e) => {
    e.preventDefault(); 

    const name = document.querySelector('#name').value;
    const email = document.querySelector('#email').value;
    const selectedDate = document.querySelector('#date').value; 
    const option = document.querySelector('input[name="form"]:checked').value;
    const urlParams = new URLSearchParams(window.location.search);
    const postId = urlParams.get('postId'); 

    try {
        const response = await fetch('http://localhost:3001/api', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ name, email, selectedDate, option, postId }), 
        });

        if (response.ok) {
            alert('Регистрация прошла успешно!');
            window.location.href = 'index.html'; 
        } else {
            alert('Ошибка регистрации.');
        }
    } catch (error) {
        console.error('Ошибка:', error);
    }
}); */